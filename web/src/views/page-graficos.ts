import { CtLit, html, property, customElement, css } from '@conectate/ct-lit';

@customElement('page-graficos')
export class PageGraficos extends CtLit {

    static styles = css`
    :host {
        display: block;
    }
    `;

    render() {
        return html``;
    }
}